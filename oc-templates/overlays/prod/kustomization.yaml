---
kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

namespace: ""

resources:
  - ./../../base

patches:
  - path: add-secret-values.patch.yaml
    target:
      kind: Secret
      name: nps-agent-secret